const styles = (theme) => ({
	root: {
		padding: 0,
		margin: 0,
		height: '100vh',
		backgroundColor: '#fafafa'
	},
	appBarContent: {
		display: 'flex',
		alignItems: 'center',
		justifyContent: 'center',
		background: '#75cfb8'
	}
});

export default styles;
